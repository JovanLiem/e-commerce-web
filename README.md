# 🛒 E-Commerce Website

This is an **E-Commerce Web Application** built using **Next.js (React)** with **TypeScript (TSX)**, styled using **Tailwind CSS** and **Mantine UI**.  
The project is bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app) and uses the **Next.js App Router**.

---

## ✨ Features

- Product listing with category filtering
- Responsive design (mobile & desktop)
- Reusable React components
- Modern UI using Mantine
- Utility-first styling with Tailwind CSS
- API-based data fetching
- Fully typed with TypeScript

---

## 🛠 Tech Stack

- **Framework**: Next.js
- **Library**: React
- **Language**: TypeScript (TSX)
- **UI Components**: Mantine
- **Styling**: Tailwind CSS
- **HTTP Client**: Axios
- **Font Optimization**: next/font (Geist)

---

## 🚀 Getting Started

Install dependencies:

```bash
npm install
# or
yarn install
# or
pnpm install
# or
bun install
```

Run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open the application in your browser:

```text
http://localhost:3000
```

The page will automatically reload when you edit the source code.

---

## 📁 Project Structure

```text
E-COMMERCE-WEB
├── public/               # Static assets
├── src/
│   ├── app/              # App Router pages
│   │   ├── about/        # About page
│   │   ├── api/          # API routes
│   │   ├── cart/         # Cart page
│   │   ├── login/        # Login page
│   │   ├── products/     # Products page
│   │   ├── register/     # Register page
│   │   ├── favicon.ico   # App favicon
│   │   ├── globals.css   # Global styles
│   │   ├── layout.tsx    # Root layout
│   │   └── page.tsx      # Home page
│   ├── components/       # Reusable UI components
│   ├── context/          # React Context (global state)
│   ├── lib/              # Helper & utility functions
│   └── types/            # TypeScript type definitions
├── .gitattributes
├── .gitignore
├── eslint.config.mjs     # ESLint configuration
├── next-env.d.ts         # Next.js type definitions
├── next.config.ts        # Next.js configuration
├── package.json
├── package-lock.json
├── postcss.config.mjs    # PostCSS configuration
├── tailwind.config.ts    # Tailwind CSS configuration
└── README.md
```

---

## ✏️ Editing the Page

You can start editing the main page here:

```text
app/page.tsx
```

Changes will be reflected instantly in the browser.

---

## 🎨 Styling

- **Tailwind CSS** is used for utility-first custom styling
- **Mantine** is used for UI components such as:
  - Buttons
  - Layout & Grid
  - Loader
  - Typography

Both are combined to create a clean, modern, and scalable UI.

---

## 📚 Learn More

- Next.js Documentation: https://nextjs.org/docs
- Learn Next.js: https://nextjs.org/learn
- Tailwind CSS Docs: https://tailwindcss.com/docs
- Mantine Docs: https://mantine.dev

---

Made using Next.js, TypeScript, Tailwind CSS, and Mantine \n

---

**Jovan Amarta Liem**
